import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		position: 'absolute',
		zIndex: 20,
	},
	content: {
		padding: 16,
		gap: 8
	}
});

export default styles;